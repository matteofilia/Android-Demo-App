const chai = require("chai")
const assert = chai.assert
const expect = chai.expect

describe("Really Really Simple Test", function() {
  describe("Assert", function() {
   it("zero equals zero", function() {
     // Don't throw any exceptions
     assert.equal(0,0)
   })
  })
})
